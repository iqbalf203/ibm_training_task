package project4;

public class App {

	public static void main(String[] args) {
		String ss = "Hello";
		String s = "";
		for (int i=ss.length()-1;i>=0;i--) {
			s += ss.charAt(i);
		}
			System.out.println(s);
	}

}
